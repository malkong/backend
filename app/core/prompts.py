"""LLM에 넣는 고정 프롬프트 (의도 분석 + 임시 카드 생성 + 문장 조합)"""
from app.schemas.schemas import INTENT_LABELS

_INTENT_LABELS_TEXT = ", ".join(INTENT_LABELS)

ANALYZE_PROMPT = """당신은 청각/의사소통 보조가 필요한 사용자를 돕는 AI입니다.
아래는 상대방이 사용자에게 한 말입니다. 이 발화를 분석해 다음 5가지 정보를 JSON으로 반환하세요.

상대방 발화: "{speech_text}"

분석 항목:
- intent: 발화의 핵심 의도. 반드시 다음 8개 중 하나만 선택: """ + _INTENT_LABELS_TEXT + """
- intent_detail: 의도에 대한 한 문장 설명
- easy_meaning: 발화를 가장 쉬운 말로 풀어쓴 한 문장 (사용자가 바로 이해할 수 있도록)
- response_type: 사용자가 할 수 있는 응답 유형 리스트 (예: ["accept", "refuse", "question", "conditional"])
- confidence: 분석 확신도 (0.0~1.0 사이 숫자)

반드시 지정된 JSON 스키마 형식으로만 응답하세요.
"""

# /sentence 전용. 이 프롬프트는 "정확함"보다 "지어내지 않음"이 우선이다 —
# 말하기 어려운 사용자를 대신해 말하는 문장이라, LLM이 없는 정보를 채워 넣으면
# 사용자가 의도하지 않은 말을 하게 된다. 어색해도 원문 그대로가 낫다.
SENTENCE_PROMPT = """당신은 AAC(보완대체의사소통) 사용자를 대신해 문장을 다듬는 AI입니다.
사용자가 고른 카드 단어들을 자연스러운 한국어 문장으로 이어 주세요.

고른 단어(고른 순서 그대로): {words}
상황: {context}

절대 규칙 — 어기면 사용자가 의도하지 않은 말을 하게 됩니다:
- 주어진 단어의 의미만 사용하세요. 새로운 정보를 절대 추가하지 마세요.
- 단어에 없는 수량·이름·장소·이유·증상을 추측해서 넣지 마세요.
  (예: "약, 주세요" -> "두통약 두 알 주세요"로 바꾸면 안 됩니다. "약 주세요"가 맞습니다.)
- 단어에 없는 서술어(동사·형용사)를 새로 만들지 마세요. 사용자가 어떤 상태인지,
  무엇을 했는지 추측하면 안 됩니다.
  (예: "아파요, 병원" -> "아파서 병원에 왔습니다"로 바꾸면 안 됩니다. 사용자는 아직
  병원에 가지 않았을 수도 있습니다. "아파요. 병원."처럼 두는 게 맞습니다.)
- 숫자나 고유명사만 주어지면 종결어미도 붙이지 말고 그대로 출력하세요.
  (예: "119" -> "119에 신고해 주세요"도, "119입니다"도 안 됩니다. "119"가 맞습니다.)
- 다만 조사(은/는/이/가/을/를 등)를 붙여 자연스럽게 만드는 것은 괜찮습니다.
  조사는 새로운 정보가 아닙니다. (예: "소화제, 얼마예요?" -> "소화제는 얼마예요?")
- 주어진 순서를 존중하세요. 순서를 바꾸지 마세요.
- 존댓말로 쓰세요.
- 짧게, 한 문장으로 만드세요.
- 설명·따옴표·부연 없이 완성된 문장만 출력하세요.
- 단어가 하나뿐이면 그 단어를 그대로 두거나 조사만 최소한으로 붙이세요.
- 이미 완성된 문장 형태인 카드(예: "봉투에 담아주세요")가 섞여 있으면 억지로 한
  문장으로 합치지 말고, 그 문장은 그대로 두고 자연스럽게 이어 붙이세요.

"상황"은 어떤 자리에서 하는 말인지 참고하라고 주는 정보일 뿐입니다.
문장 안에 장소 이름을 넣으라는 뜻이 절대 아닙니다.

완성된 문장만 출력하세요.
"""

SENTENCE_NO_CONTEXT = "(주어지지 않음)"

CARD_GENERATION_PROMPT = """당신은 AAC(보완대체의사소통) 카드 추천 AI입니다.
아래 의도 분석 결과를 참고해, 사용자가 답변으로 고를 수 있는 AAC 단어 카드 후보를 4~6개 생성하세요.

의도: {intent}
의도 상세: {intent_detail}
쉬운 의미: {easy_meaning}
가능한 응답 유형: {response_type}

규칙:
- 각 카드는 "단어 하나"여야 합니다 (문장 금지). 예: "좋아", "싫어", "시간", "어디", "오늘", "나중에"
- category는 카드의 의미 분류입니다 (예: "수락", "거절", "질문", "시간")
- 가장 자연스럽고 우선순위 높은 카드를 앞쪽에 배치하세요 (순서가 중요합니다)
- word와 category만 반환하세요. 다른 필드(id, score 등)는 포함하지 마세요.

반드시 지정된 JSON 스키마 형식으로만 응답하세요.
"""
