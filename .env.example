GEMINI_API_KEY=your_gemini_api_key_here

# STT (stt.py) 실행 환경 오버라이드. 기본값은 CPU(cpu/int8).
# STT_DEVICE=cpu
# STT_COMPUTE_TYPE=int8

# MySQL 접속 정보 (개인화 이력 저장용). MySQL 미가용이어도 서버는 기동되며 개인화만 생략됨.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=aac
